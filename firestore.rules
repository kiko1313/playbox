rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Videos: Public Read, Admin Write
    match /videos/{videoId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Files: Public Read, Admin Write
    match /files/{fileId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Settings: Public Read (for ads), Admin Write
    match /settings/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Default: Deny all
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
